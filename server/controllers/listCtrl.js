module.exports = {
    getListsByUser: async (req, res, next) => {
        try {
            const db = req.app.get('db')
            const { id } = req.params
        } catch (error) {
            
        }
    }
}